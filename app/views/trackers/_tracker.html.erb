	<li><%= tracker.day %></li>
	<%= simple_form_for [@habit, tracker], class: "tracker_form" do |f| %>
		<%= f.collection_check_boxes :success, [[true, "Success"], [false, "Fail"]], :first, :last %>
		<%= f.input :notes %>
		<%= f.submit class: "btn btn-primary" %>
	<% end %>
