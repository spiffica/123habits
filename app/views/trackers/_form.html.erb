	<%= simple_form_for [@habit, @tracker], class: "tracker_form" do |f| %>
		<%= f.collection_check_boxes :success, [[true, "Success"], [false, "Fail"]] %>
		<%= f.input :notes %>
		<%= f.submit class: "btn btn-primary" %>
	<% end %>